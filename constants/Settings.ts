// choose the unit system, either metric or imperial
export const UNITS = "metric" as "metric" | "imperial";

// set the timesteps, like "1m", "1h" and/or "1d"
export const TIMESTAMPS = ["1d"] as string[];
